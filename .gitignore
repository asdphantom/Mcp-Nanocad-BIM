# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/
*.whl
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.coverage*
htmlcov/
.coverage

# ── Python Virtual Environment ────────────────────────────────────
.venv/
venv/
env/

# ── .NET ──────────────────────────────────────────────────────────
bin/
obj/
*.pdb
*.exe
*.cache
*.nupkg
NuGet.config
packages/

# ── But keep the pre-built plugin in dist ────────────────────────
!engine/dist/
!engine/dist/*.dll

# ── nanoCAD ────────────────────────────────────────────────────────
nanoCAD/*.dwl
nanoCAD/*.dwl2
nanoCAD/*.bak
nanoCAD/*.sv$
nanoCAD/*.ac$
nanoCAD/Fonts/
nanoCAD/PlotCfg/
nanoCAD/PlotStyles/
nanoCAD/Support/
nanoCAD/Template/
nanoCAD/backup/
nanoCAD/*.log
nanoCAD/*.ini
nanoCAD/nCad.ini
nanoCAD/*.exe
nanoCAD/*.dll
nanoCAD/*.cfg
nanoCAD/*.lsp
nanoCAD/DataRO/
nanoCAD/help/
nanoCAD/Lsp/
nanoCAD/redist/
nanoCAD/UserDataCache/
nanoCAD/PGSQL/
*.dwg
*.dxf
*.bak

# ── nanoCAD full installation ────────────────────────────────
/nanoCAD/

# ── MultiCAD SDK ───────────────────────────────────────────────────
/MultiCAD_API/

# ── Old experimental code ─────────────────────────────────────────
/engine-mapi/

# ── CAD temp files ────────────────────────────────────────────────
*.tmp
*.dwl
*.dwl2
*.bak
*.sv$
Без имени0*
CSP*.tmp

# ── IDE ────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── Logs & Temp (server) ──────────────────────────────────────────
server/logs/
server/*.log
server/test_*.log
server/test_body.json
server/tmp_check_db.py
engine/CadEngine.Plugin/log.txt

# ── OS ─────────────────────────────────────────────────────────────
Thumbs.db
.DS_Store
Desktop.ini

# ── SDK & Large Binaries ──────────────────────────────────────────
*.exe
*.zip
*.7z
*.rar
NC_SDK_RU_*/
tmp_chm/

# ── Project Specific ──────────────────────────────────────────────
*.yaml
*.yml

# ── Docs Build ────────────────────────────────────────────────────
docs/_build/

# ── Config override (local only) ──────────────────────────────────
.env
.env.*
*.local

# ── OpenCode (local agent config) ────────────────────────────────
.opencode/

# ── Demo & audit scripts (excluded from repo) ────────────────────
IMPLEMENTATION_PLAN.md
server/scripts/*.py
server/scripts/check_*.*
server/scripts/count_*.*
server/scripts/test_*.*
server/scripts/validate_*.*
server/scripts/demo_bearing_bracket*.*
server/scripts/demo_pillow_block*.*

# ── Outdated docs ────────────────────────────────────────────────
docs/instructions/06_server_improvements.md

# ── GitHub workflows (removed from repo) ─────────────────────────
.github/

# ── Internal dev notes (not for publication) ───────────────────
AGENTS.md
server/start_mcp.bat
*PLAN*.md
ARTICLE.md

# ── Temporary .NET test projects ───────────────────────────────
engine/.helix_check/
engine/MapiTest/

# ── Keep .gitignore local only ──────────────────────────────────
/.gitignore

/work/
